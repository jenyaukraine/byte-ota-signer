# byte-ota-signer
```
usage: main.py [-h] {c,w} ...

Console for reading bytes and replacing bytes from another file.

options:
  -h, --help  show this help message and exit

Commands:
  {c,w}
    c         Process file for verify bytes
    w         Copy bytes from first file into second
